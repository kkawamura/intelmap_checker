require 'spec_helper'

describe IntelmapChecker do
  it 'has a version number' do
    expect(IntelmapChecker::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
